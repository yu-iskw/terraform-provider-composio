# Optional local environment values for acceptance tests.
#
# Make does not load this file. Copy and export manually:
#   cp .env.template .env
#   set -a && source .env && set +a
#   make testacc
#
# Use a dedicated Composio project API key (not production).

COMPOSIO_API_KEY=replace-me
# COMPOSIO_ENDPOINT=https://backend.composio.dev
# COMPOSIO_ORG_API_KEY=replace-me
# Optional: public HTTPS MCP URL for composio_custom_toolkit Acc tests
# COMPOSIO_ACC_CUSTOM_MCP_URL=https://mcp.example.com/mcp
