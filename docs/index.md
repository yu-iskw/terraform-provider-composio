---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "composio Provider"
description: |-
  Manage durable Composio control-plane configuration. Terraform configures projects, auth configs, and related objects. Applications authorize users and execute tools.
---

# composio Provider

Manage durable Composio control-plane configuration. Terraform configures projects, auth configs, and related objects. Applications authorize users and execute tools.

## Example Usage

```terraform
provider "composio" {
  # api_key     = var.composio_api_key
  # org_api_key = var.composio_org_api_key

  # Optional. Defaults to https://backend.composio.dev
  # endpoint = "https://backend.composio.dev"

  # Optional advanced controls
  # max_concurrent_requests = 8
  # request_timeout         = "30s"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `api_key` (String, Sensitive) Project API key sent as `x-api-key`. Falls back to `COMPOSIO_API_KEY`. Required by project-scoped resources and data sources.
- `endpoint` (String) Composio API origin. Defaults to `https://backend.composio.dev`. Override only for tests or a proxy. The provider always calls `/api/v3.1` under this origin.
- `max_concurrent_requests` (Number) Maximum in-flight HTTP requests. Defaults to 8.
- `org_api_key` (String, Sensitive) Organization API key sent as `x-org-api-key`. Falls back to `COMPOSIO_ORG_API_KEY`. Required by organization-scoped resources such as projects.
- `request_timeout` (String) Per-request timeout as a Go duration, for example `30s`. Defaults to 30s.
